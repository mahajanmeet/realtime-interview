{
  "name": "realtime-interview",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "predev": "npm run build -w @interview/shared",
    "dev": "concurrently -k -n shared,server,desktop \"npm run dev -w @interview/shared\" \"npm run dev -w @interview/server\" \"npm run dev -w @interview/desktop\"",
    "build": "npm run build -w @interview/shared && npm run build -w @interview/server && npm run build -w @interview/desktop",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "prettier": "^3.9.6",
    "typescript": "5.9"
  },
  "allowScripts": {
    "esbuild@0.27.7": true,
    "esbuild@0.25.12": true,
    "esbuild@0.28.2": true
  }
}
