# Transcription evaluation data

This directory is evaluation-only. Keep its utterances separate from any training or fine-tuning
corpus so that Word Error Rate and Technical Term Error Rate remain meaningful.

Each terminology case contains recent context, raw ASR output, expected normalized output, and the
technical terms to measure. Add real, consented interview samples here before making accuracy claims.
